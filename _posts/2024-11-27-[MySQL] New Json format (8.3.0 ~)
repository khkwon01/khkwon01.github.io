---
layout: post
---

## 1. 실행계획(explain)를 json 타입으로 쉽게 application에서 분석을 하기 위해 새로운 형태에 json 타입을 지원

   (새로운 json포맷은 반복 어떻게 구성이 되는지 또는 tree format과 어떻게 매치가 되는지를 보여줌)

```
사용방법 
set explain_json_format_version={1,2}   // 1: old format, 2: new format
explain format=JSON SELECT ~~~~
```
